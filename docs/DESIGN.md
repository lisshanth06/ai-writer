# Architecture and Design

This application uses the following tech stack

1. The user interface is a web application that uses the `Django` framework
1. The AI parts will be built using Langgraph framework - https://docs.langchain.com/
1. We will use SQLite as the SQL database and Qdrant in local mode as the Vector Database

